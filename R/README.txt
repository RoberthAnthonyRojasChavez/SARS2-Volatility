The code uses as input a log-transformed Stdev output from V calculations, and a network of co-mutable positions from the fisher's exact test code. R is the average across all the clusters.

To run FisherExact.java enter the following CLI arguments:
"[Path to file]"
"[Path to output folder]"
